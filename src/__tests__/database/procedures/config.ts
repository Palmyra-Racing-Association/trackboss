const config = {
    host: 'localhost',
    user: 'dev',
    password: 'devpass',
    database: 'pradb',
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0,
};

export default config;
